const Gameboard = (() => {
    let board = [['X', 'O', 'X'], ['X', 'X', 'X'],['O', 'O', 'O']];

    const displayBoard = () => {
        for(let i=0; i < 3;i++)
        {
            for(let j=0; j < 3;j++)
            {
                board[i][j]
            }
        }
    }
})();

const DisplayController = (() => {

})();

const Player = () => {
    
}